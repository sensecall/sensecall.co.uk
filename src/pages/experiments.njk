---
layout: base.njk
title: Experiments
permalink: experiments/
---

<div class="py-8">
    <div class="md:w-2/3">
        <h1 class="text-2xl md:text-5xl mb-4 leading-tight text-dark dark:text-white font-semibold">Experiments</h1>
        <p class="mb-4 text-base md:text-xl text-gray-700 dark:text-gray-300">
            This is where I experiment with random ideas and technologies. Some of them might go on to become full-fledged projects, some might not.
        </p>
    </div>
    <div class="grid grid-cols-1 md:grid-cols-2 gap-y-4 gap-x-8 md:gap-x-12 md:gap-y-8 mt-8">
        {% for experiment in collections.experiments %}
            <div class="mb-8">
                <h2 class="text-xl md:text-2xl mb-2 font-semibold">
                    <a href="{{ experiment.url }}"
                       class="text-blue-500 dark:text-blue-400 hover:text-blue-600 dark:hover:text-blue-300 underline inline-flex items-center gap-1"
                       target="_blank">
                        {{ experiment.title }}
                        <svg xmlns="http://www.w3.org/2000/svg"
                             class="h-5 w-5"
                             fill="none"
                             viewBox="0 0 24 24"
                             stroke="currentColor">
                            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M10 6H6a2 2 0 00-2 2v10a2 2 0 002 2h10a2 2 0 002-2v-4M14 4h6m0 0v6m0-6L10 14" />
                        </svg>
                    </a>
                </h2>
                <p class="text-gray-700 dark:text-gray-300 mb-4">{{ experiment.description }}</p>
                <div class="text-gray-500 dark:text-gray-400 italic p-4 bg-gray-100 dark:bg-gray-800 rounded-lg mt-2">
                    <h3 class="text-base font-semibold mb-1">Why did I build this?</h3>
                    <p class="text-sm md:text-base">{{ experiment.why }}</p>
                </div>
            </div>
        {% endfor %}
    </div>
</div>
