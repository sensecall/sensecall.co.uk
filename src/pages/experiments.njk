---
layout: base.njk
title: Experiments
permalink: experiments/
bodyClasses: bg-white dark:bg-gray-900
---

<div class="container mx-auto px-4 py-8">
    <h1 class="text-2xl md:text-5xl mb-4 leading-tight text-dark dark:text-white font-semibold">Experiments</h1>

    <p class="mb-4 text-base md:text-xl text-gray-700 dark:text-gray-300">
        This is where I experiment with random ideas and technologies.
    </p>

    <div class="grid grid-cols-1 md:grid-cols-2 gap-8 mt-8">
    {% for experiment in collections.experiments %}
        <div class="border border-gray-200 dark:border-gray-700 p-6 md:p-8 rounded-lg">
            <h2 class="text-xl md:text-2xl mb-2 font-semibold"><a href="{{ experiment.url }}" class="underline text-blue-500 dark:text-blue-400" target="_blank">{{ experiment.title }}</a></h2>
            <p class="text-gray-700 dark:text-gray-300 mb-4">{{ experiment.description }}</p>
            <div class="text-gray-500 dark:text-gray-400 italic p-4 bg-gray-100 dark:bg-gray-800 rounded-lg">
                <h3 class="text-lg font-semibold mb-2">Why did I build this?</h3>
                <p>{{ experiment.why }}</p>
            </div>
        </div>
    {% endfor %}
    </div>
</div>
