---
eleventyImport:
collections: ["post"]
layout: page.njk
title: Writing – Dan Sensecall
permalink: /writing/
description: A collection of articles, essays and thoughts about design, technology and the future of work.
---

<div class="py-8 lg:py-12">
    <div class="max-w-4xl">
        <h1 class="heading-l">Writing</h1>
        <p class="body-m">
            I sometimes write about things that interest me or what I'm working on.
        </p>
        {% if collections.post.length > 0 %}
            <div class="mt-8 space-y-6">
                {% for post in collections.post | reverse %}
                    <article class="border-b border-gray-200 dark:border-gray-800 pb-6">
                        <h2 class="heading-s">
                            <a href="{{ post.url }}" class="text-link">
                                {{ post.data.title }}
                            </a>
                        </h2>
                        <p class="body-s text-gray-600 dark:text-gray-400 flex flex-wrap items-center gap-2">
                            <span>{{ post.date | niceDate }}</span>
                            {% if post.data.draft or post.data.status == "draft" %}
                                <span class="body-xs inline-flex items-center rounded border border-amber-300 dark:border-amber-700 px-1.5 py-0.5 font-medium text-amber-700 dark:text-amber-300">Draft</span>
                            {% endif %}
                        </p>
                        {% if post.data.description %}
                            <p class="body-m">
                                {{ post.data.description }}
                            </p>
                        {% endif %}
                    </article>
                {% endfor %}
            </div>
        {% else %}
            <p class="body-m mt-8">
                There are no published posts yet, but I’ll add new articles here soon.
            </p>
        {% endif %}
    </div>
</div>
